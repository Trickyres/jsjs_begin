# 地图点位图片

将多稼居民委员会的实景照片放在本目录，并命名为：

```text
duojia-committee.jpg
```

刷新页面后，点击地图上的“多稼居民委员会”，照片会自动显示在页面下方的点位卡片中。

推荐使用横向照片，比例约为 `16:9` 或 `3:2`，建议宽度不少于 1200 像素。也可以使用 PNG 或 WebP，但需要同步修改 `index_renew.js` 中该点位的 `image` 文件名。
